import 'package:flutter/material.dart';

//constant colors through out the app
const Color primaryColor= Color(0xFF0AB6AB);
const Color secondaryColor= Color(0xFF151515);
const Color textPrimaryColor= Colors.white;
const Color textSecondaryColor= Colors.white70;
